
  SakuraOS live environment

  This media boots straight to the desktop. Close this terminal and use the
  installer from there.

  Wi-Fi: use the network icon in the system tray, or nmcli/nmtui from a
  terminal. This system uses NetworkManager, not iwd — iwctl will not work.

  No custom installer yet (M0). Run archinstall for a text-mode install.

